# Pontos de entrada da API!
type Query {
    ola: String
    horaAtual: String
    usuarioLogado: Usuario
    produtoEmDestaque: Produto
    numeroMegaSena: [Int!]!
    retornaUsuarios: [Usuario]
    usuario(id: ID): Usuario 
    perfis: [Perfil!]!
    perfil(id: ID): Perfil
}